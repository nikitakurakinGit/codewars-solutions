function findShort(s){
  const arr = s.split(' ')
  let length = arr[0].length
  for (let i = 0; i < arr.length; i++) {
    if (arr[i].length < length) {
      length = arr[i].length
    }
  }
  return length;
}
