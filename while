function nbYear(p0, percent, aug, p) {
    let ans = 0
    let currentPop = p0
    while (currentPop < p) {
      currentPop = Math.floor(currentPop + (currentPop * (percent/100)) + aug)                       
      ans += 1
    } 
  return ans
}
