function highAndLow(numbers){ 
  const arr = numbers.split(' ')  
  return Math.max(...arr) + ' ' + Math.min(...arr);

}


function highAndLow(numbers){
  const arr = numbers.split(' ')
    let max = Math.max(...arr)
  let min = Math.min(...arr)
  const arrTwo = [max,min]
  
  return arrTwo.join(' ')
 
}
